name: CI - Build and Push to ACR

on:
  push:
    branches:
      - main   # change if your default branch is different

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Login to ACR
      run: az acr login --name nextrowdevcr

    - name: Build and Push image
      run: |
        IMAGE_TAG=${{ github.sha }}
        docker build -t nextrowdevcr.azurecr.io/dev/webscrapper:$IMAGE_TAG .
        docker push nextrowdevcr.azurecr.io/dev/webscrapper:$IMAGE_TAG

    - name: Save image info to file
      run: echo "nextrowdevcr.azurecr.io/dev/webscrapper:${{ github.sha }}" > image-info.txt

    - name: Upload metadata for CD
      uses: actions/upload-artifact@v4
      with:
        name: image-info
        path: image-info.txt
